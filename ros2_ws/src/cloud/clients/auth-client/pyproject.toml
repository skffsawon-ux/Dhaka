[project]
name = "innate-auth-client"
version = "0.1.0"
description = "Innate auth client library: OIDC JWT acquisition and timed renewal for robots."
# requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
httpx = ["httpx>=0.24"]

[project.scripts]
innate-auth-token = "auth_client.__main__:main"
