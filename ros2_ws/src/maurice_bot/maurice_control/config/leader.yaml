/leader_arm:
  ros__parameters:
    device_name: "/dev/ttyACM0"  # Serial device name
    baud_rate: 1000000          # Communication baud rate
    control_frequency: 100.0    # Control loop frequency in Hz
    servo_ids: [1, 2, 3, 4, 5, 6]